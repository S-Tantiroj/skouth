.env
venv/
Supabase Password.txt
node_modules/

# ไฟล์ล็อกของ Word — สร้างขึ้นตอนเปิดไฟล์ .doc ค้างไว้ และหลุดเข้า git ไปแล้วครั้งหนึ่ง
# (docs/uat/~$outh-uat-dev.doc เมื่อ 18 ก.ย. 2026) เป็นไฟล์ชั่วคราวที่ไม่มีประโยชน์
# และอาจมีชื่อผู้ใช้ของเครื่องอยู่ข้างใน
~$*

# ผลดิบจาก scripts/brightdata-probe.ts — เป็นข้อมูลโปรไฟล์ของบุคคลจริง
# สคริปต์นั้นมีไว้วัดอัตราการได้ข้อมูล ไม่ใช่นำเข้า ผลจึงไม่ควรอยู่ในรีโปสาธารณะ
.brightdata-probe/
# รายการ URL ที่ใช้ทดลอง ก็ระบุตัวบุคคลได้เช่นกัน
profile-urls.txt

.next/
.superpowers/
.claude/
tsconfig.tsbuildinfo

# ชุดประเมิน: pool.js มีชื่อ ประวัติ และข้อมูลส่วนบุคคลของผู้สมัครจริง
# labels.json อ้างอิงถึงคนเหล่านั้นด้วย id — ทั้งคู่ห้ามขึ้น GitHub
# หน้า eval/label.html ไม่มีข้อมูล จึง commit ได้ตามปกติ
eval/pool.js
eval/labels.json
# rubric.json/js สร้างจากฟิลด์ของงาน ไม่มีข้อมูลผู้สมัคร แต่เป็นไฟล์ที่สร้างได้ใหม่
eval/rubric.js
